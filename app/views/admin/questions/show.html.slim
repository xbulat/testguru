h1 = t('.header', title: @question.test.title)
h2 = t('.question', body: @question.body)

h3 = t('.title')
p
  = link_to t('.add_link'), new_admin_question_answer_path(@question), class: "btn btn-primary btn-sm"

ol
  = render @question.answers

p
  =< link_to t('navigating.back'), admin_test_path(@question.test), class: "btn btn-outline-dark btn-sm"
  =< link_to t('navigating.edit'), edit_admin_question_path(@question), class: "btn btn-outline-dark btn-sm"
